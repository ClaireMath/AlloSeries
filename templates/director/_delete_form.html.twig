<form method="post" action="{{ path('director_delete', {'id': director.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('Supprimer' ~ director.id) }}">
    <button style="margin-top: 10px;" class="btn btn-danger">Supprimer</button>
</form>
