{% extends 'base.html.twig' %}

{% block title %}New Director{% endblock %}

{% block body %}
<div class="card-body" style="padding: 30px; display: flex; flex-direction: column; justify-content: center; align-items: center;">

    <h1 style="padding: 30px; text-align: center;">Create new Director</h1>

    {{ include('director/_form.html.twig') }}

    <a class="btn btn-info" href="{{ path('director_index') }}">back to list</a>
{% endblock %}
