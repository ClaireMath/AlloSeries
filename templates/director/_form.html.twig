<div class="card-body" style="display: flex; justify-content: center; align-items: center">
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button style="width: 160px;"  class="btn btn-info">{{ button_label|default('Enregistrer') }}</button>
{{ form_end(form) }}
