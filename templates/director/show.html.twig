{% extends 'base.html.twig' %}

{% block title %}Director{% endblock %}

{% block body %}
    <h1>Director</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ director.id }}</td>
            </tr>
            <tr>
                <th>First_name</th>
                <td>{{ director.firstName }}</td>
            </tr>
            <tr>
                <th>Last_name</th>
                <td>{{ director.lastName }}</td>
            </tr>
            <tr>
                <th>Date_of_birth</th>
                <td>{{ director.dateOfBirth ? director.dateOfBirth|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Bio</th>
                <td>{{ director.bio }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('director_index') }}">back to list</a>

    <a href="{{ path('director_edit', {'id': director.id}) }}">edit</a>

    {{ include('director/_delete_form.html.twig') }}
{% endblock %}
